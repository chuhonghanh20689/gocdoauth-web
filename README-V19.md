# Góc Đồ Auth v19

Homepage content is stored in Supabase `pages.content` as JSON.

## One-time DB setup
Run `SUPABASE-HOMEPAGE-CONTENT.sql` in Supabase SQL Editor.

Then use Admin → Nội dung → Trang chủ to edit homepage text.
Branding images remain in `site_settings` and Storage.
