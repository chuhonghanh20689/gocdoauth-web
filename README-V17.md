# Góc Đồ Auth v17

- Hero headline is now editable from Admin → Nội dung → Trang chủ.
- Hero headline supports line breaks.
- Hero headline is smaller and more balanced over the banner.
- Existing Supabase data, products, logo, banner and category images are preserved.

Keep the existing `.env.local` file.
